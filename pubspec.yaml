name: app
description: Carros Flutter application.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  get_it: ^7.1.3

  fluro: ^2.0.3

  http: ^0.13.3
  rxdart: ^0.27.0
  flutter_secure_storage: ^4.2.0
  cached_network_image: ^3.1.0-alpha.1
  connectivity: ^3.0.6
  dartz: ^0.8.9
  equatable: ^2.0.2

  cupertino_icons: ^1.0.3

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  mockito: ^5.0.9
  build_runner: ^2.0.4

flutter:
  uses-material-design: true

  assets:
    - assets/mock/
